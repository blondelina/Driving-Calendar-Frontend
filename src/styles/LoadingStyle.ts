import {
    StyleSheet
  } from 'react-native';

const loadingStyle = StyleSheet.create({
    loading: {
      display: 'flex',
      justifyContent: 'center',
      flexDirection: 'column',
      alignItems: 'center',
      height: '100%'
    }
  });
  
  export { loadingStyle }